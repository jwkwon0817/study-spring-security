jwt:
  secret: thisisanddljwtsecuritytokeninspringsecurityjwtauthenticationfilter

# Application port
server:
  port: 8080

# Spring Boot Config
spring:
  redis:
    host: localhost
    port: 6379
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/security
    username: root
    password: anddldev0906!!
  jpa:
    properties:
      hibernate:
        # JPA Query Console
        show_sql: false

        # SQL Format
        format_sql: true
        ddl-auto: validate
    generate-ddl: true
    database-platform: org.hibernate.dialect.MariaDBDialect
